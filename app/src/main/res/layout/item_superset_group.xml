<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/superset_card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="8dp"
    android:layout_marginVertical="4dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    app:strokeWidth="2dp"
    app:strokeColor="@color/colorPrimary">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <!-- Заголовок суперсета -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingBottom="8dp">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_fitness"
                android:contentDescription="Суперсет"
                app:tint="@color/colorPrimary" />

            <TextView
                android:id="@+id/superset_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:text="Суперсет"
                android:textAppearance="?attr/textAppearanceSubtitle1"
                android:textColor="@color/colorPrimary"
                android:textStyle="bold" />

            <!-- Убрали кнопку меню суперсета -->

        </LinearLayout>

        <!-- Контейнер для упражнений суперсета -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/superset_exercises_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:nestedScrollingEnabled="false" />

        <!-- Информация о суперсете -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="8dp"
            android:padding="8dp"
            android:background="@drawable/rounded_background_light"
            android:gravity="center">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="💪"
                android:textSize="16sp"
                android:layout_marginEnd="4dp" />

            <TextView
                android:id="@+id/superset_info"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Выполните упражнения друг за другом без отдыха"
                android:textAppearance="?attr/textAppearanceCaption"
                android:textColor="@color/text_secondary"
                android:textStyle="italic" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
